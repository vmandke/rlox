// Towers of Hanoi — recursive, 4 disks
fun hanoi(n, from, to, via) {
  if (n == 1) {
    print from + " -> " + to;
    return nil;
  }
  hanoi(n - 1, from, via, to);
  print from + " -> " + to;
  hanoi(n - 1, via, to, from);
}

hanoi(4, "A", "C", "B");
